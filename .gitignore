# Swift Package Manager
.build/
.swiftpm/

# Xcode
*.xcworkspace
*.xcuserdata
DerivedData/

# macOS
.DS_Store

# Environment (local)
.env
*.swp
*~

# Generado por `swift run UTMReferenceDump` (tools/check_utm.py --file lo consume;
# es un artefacto derivado y determinista, no una fuente de verdad versionada).
tools/utm_reference.txt